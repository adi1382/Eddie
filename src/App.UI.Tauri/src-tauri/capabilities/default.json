{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set: the user interface only talks to the Eddie engine through the application commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
